programa
numero a,b,c,d.
texto e, f.
escreva("Programa Teste").
escreva ("Digite A").
leia (a).
escreva ("Digite B").

a = 123
e = "123"

// b="asd" // -> Gera exceção
// f=5 // -> Gera exceção

while (a<b)
    {
     c := a + b.
    }

escreva ("C e igual a").
escreva (c).
d := c * a + b.
escreva ("D e igual a").
escreva (d).
fimprog.